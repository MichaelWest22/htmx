{
  "name": "htmx-vite-test",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "test": "vite build && node verify.js && npm run cleanup",
    "cleanup": "rm -rf node_modules dist package-lock.json"
  },
  "devDependencies": {
    "vite": "*"
  }
}
