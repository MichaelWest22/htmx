{
  "name": "htmx-webpack-test",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "test": "webpack --mode production && node verify.js && npm run cleanup",
    "cleanup": "rm -rf node_modules dist package-lock.json"
  },
  "devDependencies": {
    "webpack": "*",
    "webpack-cli": "*"
  }
}
